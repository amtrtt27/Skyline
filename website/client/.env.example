# Base URL for the Express API
VITE_API_BASE_URL=http://localhost:3001/api

# Demo Mode defaults ON so the MVP runs immediately (no server needed).
# Set to false if you want to use the Express API.
VITE_DEMO_MODE=true

# Google Maps API Key (leave blank to force placeholder maps)
VITE_GOOGLE_MAPS_API_KEY=
